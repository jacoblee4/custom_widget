// Only trigger based on context
if (current.getTableName() == 'sysapproval_approver') {
    gs.info('SIGNATURE TESTING - Approval State: ' + current.state);
    gs.info('SIGNATURE TESTING - Document Table: ' + documentTableName);
    gs.info('SIGNATURE TESTING - Has Child Record: ' + (childRecord != null));
    
    // From approval record - check if it's rejected
    if (current.state != 'rejected') {
        return;
    }
